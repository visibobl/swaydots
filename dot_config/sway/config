# Read `man 5 sway` for a complete reference.

#########
# Execs #
#########
exec --no-startup-id autotiling
exec --no-startup-id dunst
exec_always wl-paste --type text --watch clipman store
# exec_always wl-paste --type image --watch clipman store
exec_always xrandr --output DP-1 --primary

############
# Defaults #
############

# Use Mod1 for Alt.
set $mod Mod4

# Terminal emulator
set $term kitty
# Application launcher
set $menu rofi -show drun
# File browser
set $explorer nautilus
# Browser
set $browser zen-browser 
# Editor
set $editor code

############
# Monitors #
############

focus output DP-1
# Main monitor
output DP-1 res 1920x1080@180Hz pos 1080 800 
# Monitor 2
output HDMI-A-1 res 1920x1080@74.97Hz pos 0 0 transform 90
# You can get the names of your outputs by running: swaymsg -t get_outputs



### Idle configuration
#
# Example configuration:
#
exec swayidle -w \
          timeout 180 'swaylock -f' \
#          timeout 600 'systemctl suspend' \
#          before-sleep 'swaylock -f -c 000000'
# This will lock your screen after 300 seconds of inactivity, then turn off
# your displays after another 300 seconds, and turn your screens back on when
# resumed. It will also lock your screen before your computer goes to sleep.

#########
# Input #
#########

# Mouse
input type:pointer {
    accel_profile flat
    pointer_accel -0.3
}

# Keyboard
input type:keyboard {
    repeat_delay 300
    repeat_rate 40
}
# Read `man 5 sway-input` for more information about this section.

##############
# Workspaces #
##############

# Bind workspaces to correct monitors
workspace 1 output DP-1
workspace 3 output DP-1
workspace 5 output DP-1
workspace 7 output DP-1
workspace 9 output DP-1

workspace 2 output HDMI-A-1
workspace 4 output HDMI-A-1
workspace 6 output HDMI-A-1
workspace 8 output HDMI-A-1
workspace 10 output HDMI-A-1

#########
# Binds #
#########
include ~/.config/sway/binds


# Refresh bar together with sway
bar {
   swaybar_command waybar
}
# Read `man 5 sway-bar` for more information about this section.

#########
# Theme #
#########
include ~/.config/sway/theme


# include /etc/sway/config.d/*